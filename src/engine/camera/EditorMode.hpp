#pragma once

namespace fadix
{
enum class EditorPlayMode
{
    Edit,
    Play,
    Paused
};

enum class ViewportCameraMode
{
    Edit,
    Game
};

enum class ViewportProjectionMode
{
    Perspective,
    Ortho2D
};
}
